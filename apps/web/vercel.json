{
  "framework": "nextjs",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @signatureops/db generate && pnpm --filter @signatureops/outlook-addin build && pnpm --filter @signatureops/web build"
}
