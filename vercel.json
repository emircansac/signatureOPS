{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @signatureops/db generate && pnpm --filter @signatureops/outlook-addin build && pnpm turbo build --filter=@signatureops/web",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':!*.md'"
}
